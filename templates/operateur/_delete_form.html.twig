<form method="post" action="{{ path('app_operateur_delete', {'id': operateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operateur.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash-fill"></i> Supprimer</button>
</form>
