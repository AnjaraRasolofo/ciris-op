{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="d-flex mt-3">
        <button class="btn btn-primary me-2"><i class="bi bi-pencil-square"></i> {{ button_label|default('Enregistrer') }}</button>
        
        {% if conge.id is not null %}
           {{ include('conge/_delete_form.html.twig') }}
        {% endif %}
    </div>
{{ form_end(form) }}
